{
  "comment": "Скопируйте в config/protected-ips.json и впишите адреса, которые НИКОГДА не должны уехать мимо VPN — например IP вашего VPN-сервера. Файл в .gitignore, в репозиторий не попадает. Если списка нет, macOS-синк работает без этой проверки.",
  "protected_ips": [
    "203.0.113.10"
  ]
}
